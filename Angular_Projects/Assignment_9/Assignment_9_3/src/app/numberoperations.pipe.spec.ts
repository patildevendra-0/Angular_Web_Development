import { NumberoperationsPipe } from './numberoperations.pipe';

describe('NumberoperationsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberoperationsPipe();
    expect(pipe).toBeTruthy();
  });
});
